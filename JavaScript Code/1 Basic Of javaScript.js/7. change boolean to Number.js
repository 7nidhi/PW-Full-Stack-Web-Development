//Convert a boolean to a number. 

let a=true;
let b=false;

console.log(`the value of a and b before conversion:${a},${b}`);

let y=Number(a);
let z=Number(b);

console.log(`the value of a and b after conversion:${y},${z}`);