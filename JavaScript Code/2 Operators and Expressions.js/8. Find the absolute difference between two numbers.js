//Find the absolute difference between two numbers.
function getAbsDiff(x,y){
    return Math.abs(x-y);
}

let a=-10;
let b=7
console.log(`Absolute differnece between ${a} and ${b} is ${getAbsDiff(a,b)}`);